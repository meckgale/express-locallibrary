<h1><%= title %></h1>
<% if (genre_list.length) { %>
<ul>
  <% genre_list.forEach(function(genre) { %>
  <li>
    <a href="<%= genre.url %>" style="text-decoration: none"
      ><%= genre.name %></a
    >
  </li>
  <% }); %>
</ul>
<% } else { %>
<p>There are no genres.</p>
<% } %>
